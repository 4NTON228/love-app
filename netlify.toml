[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# API редирект должен быть ПЕРЕД SPA редиректом
[[redirects]]
  from = "/api/send-push"
  to = "/.netlify/functions/send-push"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
